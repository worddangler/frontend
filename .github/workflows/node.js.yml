name: worddangler-frontend

on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]

jobs:
  lint:
    steps:
      runs-on: ubuntu-latest
      - uses: actions/node-composite-action@v1
      - run: npm run lint
  test:
    steps:
      runs-on: ubuntu-latest
      - uses: actions/node-composite-action@v1
      - run: npm test -- --coverage
  build:
    needs: [lint, test]
    runs-on: uburtu-latest
    steps:
      - uses: actions/node-composite-action@v1
      - run: npm run build
