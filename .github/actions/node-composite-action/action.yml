name: 'node composite'
runs: 
  using: "composite"
    steps:
      - uses: actions/checkout@v3
        uses: actions/setup-node@v3
        with:
          node-version: "16.X"
          cache: "npm"
      - run: npm ci 
